spring:
  application:
    name: vantrack
  jackson:
    date-format: yyyy-MM-dd
    time-zone: America/Sao_Paulo
  flyway:
    enabled: true
    validate-on-migrate: true
    locations:
      classpath:db/migration
    schemas: public
server:
  port: 8080

jwt:
  secret: ${JWT_SECRET}